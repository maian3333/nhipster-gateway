# Environment configuration
BACKEND_ENV=dev

# Vault Configuration - IMPORTANT: Use environment variables for security
VAULT_TOKEN=your-vault-token-here
VAULT_URI=http://appf4s.io.vn:8200

# Optional: Override Vault settings
# VAULT_KV_ENABLED=true
# VAULT_KV_BACKEND=secret
# VAULT_KV_APPLICATION_NAME=common-kafka
# VAULT_CONFIGURATION_INFRASTRUCTURE=secret/infrastructure

# Database Configuration (if using Vault secrets)
# DATABASE_URL=postgresql://user:password@localhost:5432/gateway

# Other secrets (typically stored in Vault)
# JWT_SECRET=your-jwt-secret
# OAUTH2_CLIENT_SECRET=your-oauth2-client-secret

# SECURITY NOTE: Never commit actual vault tokens to version control!
# Use environment variables or secret management systems.